import { sveltekit } from '@sveltejs/kit/vite';
import { defineConfig } from 'vitest/config';
import { readFileSync } from 'fs';

const pkg = JSON.parse(readFileSync('./package.json', 'utf-8'));

export default defineConfig({
	plugins: [sveltekit()],
	/*
	 * Словники локалей — окремим чанком, і це умова ПРАВДИВОСТІ гейта бюджету,
	 * а не оптимізація завантаження (PERFORMANCE-v9 § 1.1
	 * `PERF-BUDGET-CODE-VS-DATA`).
	 *
	 * `scripts/check-bundle.mjs` ділить критичний шлях на «код» і «дані» ПО
	 * ЧАНКАХ: знайшов у чанку маркер словників — увесь чанк порахував як
	 * контент і зі стелі коду виключив. Хто саме лежить поруч зі словниками,
	 * вирішує Rollup. Заміряно 2026-09-10: разом із ними в один чанк потрапила
	 * бібліотека валідації на 17.5 КБ gzip, і її вага рахувалася як контент —
	 * тобто гейт, який стежить, щоб код не ріс непомітно, непомітно ж і не
	 * рахував частину коду.
	 *
	 * Байтів на сторінці це правило не змінює: словники імпортуються статично,
	 * тож чанк лишається в `modulepreload` — рівно ті самі байти, лише окремим
	 * файлом. Змінюється те, що тепер їх можна порахувати окремо чесно.
	 *
	 * Прибирати не можна: `check:bundle` червоніє «чанк даних імпортує код» —
	 * умову перевірено зворотним експериментом.
	 */
	build: {
		rollupOptions: {
			output: {
				manualChunks(id) {
					return id.includes('/src/lib/i18n/locales/') ? 'locales' : undefined;
				}
			}
		}
	},
	define: {
		__APP_VERSION__: JSON.stringify(pkg.version),
	},
	test: {
		include: ['src/**/*.{test,spec}.{js,ts}'],
		environment: 'jsdom',
		globals: true,
		/*
		 * Типові 5000 мс — не запас, а межа, якої цей набір торкається на
		 * навантаженій машині (AI-AGENT-PITFALLS-v8 § 1.2: гейт, що червоніє без
		 * дефекту, вимикають, і тоді він не ловить уже нічого).
		 *
		 * Заміряно 2026-08-28 двома прогонами того самого чистого дерева:
		 *
		 *   холодний  — 3 файли впали, `environment 1057.27s`, разом 103.86s;
		 *   теплий    — 39/39 зелених, `environment 63.10s`, разом 16.45s.
		 *
		 * Ті самі два файли окремо: `4.31s`, 18/18 зелених. Тобто дефекту не було
		 * ЖОДНОГО — набір червонів від конкуренції за підняття jsdom.
		 *
		 * Причина довгих перевірок відома й законна: `storage`, `logService` і
		 * `storageMigration` мусять робити `vi.resetModules()` + `await import()`
		 * ПІСЛЯ підстановки сховища (див. PROJECT-CONTEXT.md, «vi.stubGlobal не діє
		 * на модуль, який уже імпортовано»). Кожен такий імпорт наново
		 * трансформує граф модулів; найповільніша перевірка в спокої — 1013 мс,
		 * тобто типовий поріг давав лише п'ятикратний запас.
		 *
		 * 20000 — це запас, а не послаблення: таймаут ловить ЗАВИСАННЯ, а зависання
		 * нескінченне й однаково буде спіймане. Число міняє тільки те, скільки
		 * чекати на справді зламану перевірку.
		 */
		testTimeout: 20000,

		/*
		 * Той самий поріг для ХУКІВ, і це не симетрія заради симетрії.
		 *
		 * `testTimeout` не поширюється на `beforeEach`/`beforeAll`: у них свій
		 * бюджет, і типовий — 10000 мс. Тобто підняття вище закрило рівно
		 * половину випадків, а найдорожча робота цього набору живе саме в хуці:
		 * `hotkeys.test.ts` робить `vi.resetModules()` + `await import()` у
		 * `beforeEach`, бо `vi.stubGlobal` не діє на вже імпортований модуль.
		 *
		 * Заміряно 2026-09-10, повний прогін на цій машині:
		 *
		 *   npm run test:unit → 1 файл впав, 44 зелених, `environment 655.09s`;
		 *     Error: Hook timed out in 10000ms
		 *       ❯ src/lib/services/hotkeys.test.ts:97:2  beforeEach(async () => {
		 *   той самий файл окремо → 22/22 зелених за 5.02s.
		 *
		 * Дефекту не було; червонила конкуренція за підняття jsdom — рівно те, що
		 * описано абзацом вище, лише в хуці. Це AI-AGENT-PITFALLS-v9 § 1.2 у
		 * чистому вигляді: гейт, який падає без дефекту, привчає не дивитися на
		 * червоне. Поки поріг стояв тільки на тестах, «червоне» в CI означало
		 * «перезапусти».
		 *
		 * Умову «hookTimeout оголошений і не менший за testTimeout» тримає
		 * `src/test-runners.test.ts`.
		 */
		hookTimeout: 20000
	}
});
