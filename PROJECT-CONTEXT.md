# Контекст проєкту: DigitalWorkshop

Персональний шар над пакетом v8 (`sveltekit-canon/selection_criteria/v8`).
Пакет описує, як має бути взагалі; цей файл — що обрано саме тут і чому.

## Базові параметри

| Параметр | Значення |
|---|---|
| Профіль | **static** |
| Adapter | `@sveltejs/adapter-static` |
| Хостинг | GitHub Pages, project page акаунта `Alik532UA` |
| Origin | `https://alik532ua.github.io` |
| base path | `/DigitalWorkshop` |
| Порт dev-сервера | 5198 (`dw-dev` у `.claude/launch.json` кореня GitHub) |
| Спільний origin з іншими застосунками? | **так** |
| PROJECT_PREFIX | `digitalworkshop_` |

## Реєстр префіксів на спільному origin

| Застосунок | Префікс |
|---|---|
| DigitalWorkshop | `digitalworkshop_` |
| CV | `cv-svelte_` |
| as5.odesa.ua | `as5.odesa.ua_` |
| MindStep | `mindstep_` |
| Slovko | `slovko_` |

`storage.clear()` перебирає ключі за префіксом і **ніколи** не викликає
`localStorage.clear()`.

## Прийняті рішення

| Питання | Обрано | Причина | Дата |
|---|---|---|---|
| Бібліотека i18n | **власна** (`LanguageState.svelte.ts` + 42 словники `.ts`) | паритет тримає анотація `: Translations` і Zod-схема; бібліотека не дала б нічого понад це | до 2026-08 |
| Мови | 42 словники, типова `uk` | та сама архітектура, що в CV — файли обох проєктів збігаються поіменно | до 2026-08 |
| Маршрут мови | `[[lang=lang]]` з матчером у `src/params/lang.ts` | без матчера сегмент ковтав би `/2026-04/`; невідома мова дає 404, а не тихий відкат на англійську | до 2026-08 |
| Теми | три: світла, темна, `colorful` | — | до 2026-08 |
| Акцентний колір | приходить динамічно з `+layout.svelte` (`--accent-primary` інлайном) | колір слідує за активною вкладкою | до 2026-08 |
| Аналітика | GA4, рантайм-інжект | prerendered сторінки | до 2026-08 |
| Версіонування | автобамп у pre-commit hook (husky) | версія в `static/app-version.json`, лише поле `version` | до 2026-08 |
| Одиниця версії | **бамп на коміт** | один автор, деплой на кожен push; номер свідомо є лічильником, а не позначкою випуску (VERSIONING-v8 § 1.2) | 2026-08-16 |
| E2E | **немає** | єдина сторінка з вкладками; Playwright не стоїть | 2026-08-16 |
| CSP: `unsafe-inline` у `script-src` | **прибрано** | поки він стояв, SvelteKit не додавав хешів узагалі — політика дозволяла будь-який інлайн-скрипт. Скрипт першого кадру перенесено під `%sveltekit.head%` і хешується зі свого файлу | 2026-08-16 |
| Поріг `npm audit` | **усі залежності**, не `--omit=dev` | 3 low сидять у dev-гілці, гейт із ними зелений; звужувати обсяг означало б перестати бачити те, що видно. Відхилення від GATE-AUDIT записане в коментарі поруч із кроком | 2026-08-16 |
| Закінчення рядків | `.gitattributes` із `* text=auto eol=lf` | без нього робоче дерево на Windows отримувало CRLF, а CI (Linux) — LF, і гейти, які читають власні джерела текстом, давали різний вердикт на тому самому коміті. Один випадок уже стався — див. «Історія рішень» | 2026-08-19 |
| Початкова тема без збереженого вибору | зі `prefers-color-scheme`, і НЕ записується у сховище | скрипт першого кадру читає системну схему; літерал `'dark'` у контролері розходився з ним і темнив сторінку після гідрації. Відсутність запису — умова того, що підписка на схему ОС узагалі працює (UI-UX-v8 § 1.4) | 2026-08-19 |
| Медіа в `static/` | підпапки `audio/` і `video/` | `images/` і `fonts/` уже були; `sea.ogg` і `sea_4_av1.webm` лишалися в корені поруч із `robots.txt` без жодної позначки, чому вони виняток | 2026-08-19 |

## Обрані optional-файли пакету

`I18N`, `ANALYTICS`, `DEPENDENCIES`, `VERSIONING`, `DOCUMENTATION` (`docs/`).
Не застосовуються: `AUTH-FORM`, `SCROLLBAR`, `AI-PROVIDERS`, `OBSERVABILITY`.

## Що не перевіряється автоматично

| Правило | Чому перевірки немає | План |
|---|---|---|
| E2E-сценарії, axe-аудит | Playwright у проєкті не стоїть | — |
| Консоль браузера на зібраному сайті | машиною не міряється взагалі (AI-AGENT-PITFALLS-v8 § 2.1). `check:build` доводить, що політика покриває кожен інлайн-скрипт, але не те, що жоден ресурс не заблоковано | ручний прохід перед релізом |
| Контраст тем | статично не перевіряється | окремий прохід; `:focus-visible` уже закрито — див. `src/focus-visible.test.ts` |
| `$props.id()` замість лічильників для `id` | звернень нуль, але й `aria-describedby`/`for` у проєкті немає — форм тут нема | з'явиться разом із першою формою |
| Розмір touch-цілей (ACCESSIBILITY-v8 § 10.3) | вимагає браузера з в'юпортом | разом із E2E, якщо він колись з'явиться |

## Відомі відхилення, які лишаються навмисно

Знайдено аудитом 2026-08-19. Кожне з них — не «не помітили», а рішення, для
якого потрібен погляд на живий сайт або продуктовий вибір, тобто те, чого
автономна правка зробити не може.

| Відхилення | Правило | Чому не змінено |
|---|---|---|
| `storage.set("backgroundType", …)` у `BackgroundState.set()` — запис, який не читає ніхто: `init()` бере фон із `?bg=` або з типового для вкладки | CODE-QUALITY-v8 (мертвий код), STORAGE-NAMESPACE-v8 | Вибір фону виглядає збереженим і не відновлюється. Обидва виходи змінюють поводження: дописати читання — фон почне переживати перезавантаження (архівний маршрут виглядатиме інакше); прибрати запис — функція зникне остаточно. Це продуктове рішення |
| `document.documentElement.style.colorScheme = 'light'` у `ThemeState.apply()` | UI-UX-v8 § 1.2 (HIGH) | Мета-тег навмисно тримає `light dark` (за це стежить `security-canon`), а цей інлайновий стиль його перекриває — обчислене значення для світлої теми стає `light`, тобто рівно те, від чого § 1.2 і застерігає (Force Dark Mode на Android Chrome). Прибрати не можна навпомацки: `color-scheme: light dark` віддасть системні смуги прокрутки й контроли темними на світлій темі в темній ОС. Потрібен погляд на пристрої |
| Кнопка звіту логів (`LogCopyButton`) видима лише при `dev` | DEBUGGING-v8 (ціна ігнорування: «лог із чужого пристрою не зібрати») | Уся редакція PII у `logService` написана під те, що звіт НАДСИЛАЄ користувач; при `dev &&` жоден відвідувач кнопки не побачить. Показати її у продакшні — продуктове рішення: червона позначка з кількістю помилок на робочому сайті |
| `src/routes/[[lang=lang]]/+page.svelte` — 1482 рядки при орієнтирі 400 | PROJECT-STRUCTURE-v8 § 7 (MEDIUM) | Розділення на секції зачіпає всю розмітку головної сторінки; перевірити результат можна лише очима на живому сайті. `SeaPageState` (565), `Header` (559), `ClockOverlay` (522) і `TopControls` (519) — те саме при орієнтирі 300 |
| Фонове відео 27 МБ із `autoplay` | PERFORMANCE-v8 | Це головний елемент оформлення сторінки, а не картинка, яку можна відкласти. Зменшення означає перекодування або інший кадр — робота з матеріалом, не з кодом |

## Перевірки, які тут є

Числа отримано `npm run test:unit`, `npx eslint . -f json` і `npm audit --json`
2026-08-19.

| Гейт | Де | Що ловить |
|---|---|---|
| `npm run lint` | CI | eslint; 66 попереджень, 0 помилок |
| `npm run check` | CI | `svelte-check`, 0 помилок |
| `npm run test:unit` | CI | 28 файлів, 219 перевірок |
| `npm run check:build` | CI, після `build` | єдиний гейт по зібраному HTML: мова кожної сторінки, canonical, noindex, hreflang, CSP і хеші інлайн-скриптів, robots↔sitemap |
| `git diff --exit-code` | CI, після `build` | збірка змінила відстежуваний файл |
| `npm audit --audit-level=high` | CI | вразливості залежностей (усіх, не лише прод — див. «Прийняті рішення») |

Файли перевірок (28): `analytics-canon`, `app-html-lang`, `ci`,
`close-button-conventions`, `css-inert-properties`, `css-variables`,
`dependencies`, `eslint-baseline`, `focus-visible`, `i18n-canon`,
`image-loading`, `listener-cleanup`, `logging-conventions`, `orphan-assets`,
`security-canon`, `static-layout`, `svelte-ignore-conventions`, `test-runners`,
`testid-conventions`, `version`, `lib/controllers/theme-init`,
`lib/controllers/theme-param`, `lib/i18n/LanguageState`,
`lib/i18n/errorMessages`, `lib/services/logService`, `lib/services/storage`,
`lib/services/storageMigration`, `test-support/source-text`.

Одинадцять із них додано 2026-08-19, і кожен закриває клас дефектів, який до
того не міг червоніти ніде:

| Гейт | Що стало неможливим приховати |
|---|---|
| `logging-conventions` | `console.*` поза `logService` — запис, який не потрапляє у звіт, що надсилає користувач |
| `listener-cleanup` | `addEventListener` без способу зняти підписку |
| `css-inert-properties` | властивість grid на багатоколонковому блоці: валідна, не діє, попередження не буває |
| `image-loading` | `<img>` без явного `loading`; `fetchpriority="high"` разом із `loading="lazy"` |
| `orphan-assets` | ресурс у `src/lib/assets/`, який не імпортує ніхто |
| `static-layout` | медіа в корені `static/`; посилання `{base}/…` без файлу на диску |
| `svelte-ignore-conventions` | заглушене a11y-попередження без коментаря-обґрунтування |
| `analytics-canon` | плейсхолдер GA, перевірений підрядком; домен аналітики, відсутній у CSP |
| `theme-init` | початкова тема, що розходиться зі скриптом першого кадру; мертва підписка на схему ОС |
| `storageMigration` | виняток зі сховища, що обриває `onMount` кореневого макета |
| `test-support/source-text` | поводження спільного прибирача коментарів, від якого залежать чотири гейти |

## Легасі-зони та списки винятків

| Список | Де живе | Записів | Причина |
|---|---|---|---|
| `CROSS_COMPONENT` | `src/css-variables.test.ts` | 3 | `--accent-primary`, `--accent-primary-rgb`, `--dynamic-bg` ставить батько інлайном, читають діти через успадкування |
| `--dropdown-bg` без варіанта для темної теми | `src/app.css` | 1 | оголошений у `:root` тим самим значенням, яке раніше було фолбеком, щоб нічого не зрушило. У темній темі біла заливка виглядає чужою поруч із `--card-bg: rgba(0,0,0,0.2)` — дати темній темі власне значення це зміна вигляду, а не виправлення дефекту |

## Історія рішень, які легко скасувати помилково

- **`tests/core.spec.ts` видалений 2026-08-16.** Файл лежав від першого коміту,
  імпортував `@playwright/test`, якого немає в залежностях, не мав конфігу,
  не потрапляв у маску Vitest і починався з `// @ts-nocheck`. Локатори, які він
  перевіряв (`lang-switcher`, `lang-uk`), у `src/` не зустрічаються. Повертати
  його без Playwright, конфігу й актуальних локаторів немає сенсу;
  `src/test-runners.test.ts` тепер червоніє на такому файлі.
- **Дата у звіті логів — ISO, не `toLocaleString()`.** Звіт читає той, хто
  розбирає збій, а не відвідувач, який його скопіював; `toLocaleString()`
  форматує в локалі браузера, і на сайті з 42 мовами це не теоретично.
- **`+error.svelte` існує з 2026-08-15.** Цей файл до 2026-08-16 стверджував
  протилежне й пропонував його «додати». Мова там береться з `<html lang>`, а не
  з `getLanguage()`, навмисно: коли помилка сталася в самому макеті, SvelteKit
  рендерить сторінку помилки БЕЗ нього, і читання контексту впало б саме там.
- **`unsafe-inline` у `script-src` назад не повертається.** Він виглядає як
  нешкідливий запасний варіант, а насправді вимикає хешування SvelteKit цілком:
  за його наявності в зібраній політиці немає жодного `sha256-`. Тримає
  `src/security-canon.test.ts`.
- **`.geminiignore` не виключає конфіги проєкту.** Вони там були, і саме з них
  асистент дізнається профіль: адаптер, CSP, `base`, наявні скрипти й гейти.
- **Числа в документації та в коментарях `eslint.config.js` беруться командою.**
  Попередній набір був названий з пам'яті й розійшовся з дійсністю на двадцять:
  у сумі обіцяв 87 попереджень при реальних 67 (AI-AGENT-PITFALLS-v8 § 5.5).
  2026-08-19 те саме сталося з `npm audit`: у цьому файлі й двічі в
  `deploy.yml` стояло «4 low» при трьох реальних. Число зі звіту старіє саме
  тоді, коли робота йде добре.
- **`.gitattributes` не прибирається.** Він виглядає як косметика, а тримає
  умову, за якої гейти взагалі щось означають. До 2026-08-19 його не було, і
  `src/security-canon.test.ts` — той, що стежить за `unsafe-inline`, — був
  ЧЕРВОНИЙ на Windows і ЗЕЛЕНИЙ у CI на тому самому коміті: він різав коментарі
  регуляркою з `$` без прапорця `m`, а `.` у JS не збігається з `\r`.
- **Відрізання коментарів у гейтах — одна функція, `src/test-support/source-text.ts`.**
  До 2026-08-19 їх було чотири різні копії, і кожна вже хоч раз давала хибний
  вердикт: гейт червонів на коментарі, який ЦИТУЄ ту саму конструкцію, від якої
  захищає. Розводити копії знову означає повторити це вп'яте.
- **Тема від системи застосовується БЕЗ запису у сховище.** Запис тут виглядає
  нешкідливим і вбиває підписку на `prefers-color-scheme` цілком: умова
  «користувач не обирав тему» після першого ж завантаження стає хибною назавжди.
  Тримає `src/lib/controllers/theme-init.test.ts`.
